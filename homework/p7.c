#include <stdio.h>
void main()
{ int h, w,per,area;
    h=7;
    w=5;
    per=(h+w)*2;
    area=(h*w);
    printf("the perimeter of rectangle is %d \n", per);
    printf("the area of square %d", area);
}