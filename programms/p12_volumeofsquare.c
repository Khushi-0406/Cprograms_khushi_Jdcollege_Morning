#include <stdio.h>
void main()
{
    int side , volume;
    printf("enter the side of square:-");
    scanf("%d",&side);
    volume=side*side*side;
    printf("the vloume of square is %d", volume);

}