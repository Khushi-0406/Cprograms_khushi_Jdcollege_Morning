#include <stdio.h>
void main()
{
    int Area, l, b;
    printf("enter the length:-");
    scanf("%d", &l);
    printf("enter the breadth:-");
    scanf("%d",&b);
    Area= l*b;
    printf("the area of rectangle:- %d", Area);

}